# Who Am I

Онлайн-игра «Кто я?» для компании друзей.

Игроки подключаются к одному лобби, получают цель, загадывают друг другу слова и пытаются угадать, кто они.

---

## 🧠 Логика игры

- Один игрок создаёт лобби (хост)
- Остальные подключаются по ID лобби
- После старта:
  - список игроков перемешивается случайным образом
  - применяется логика `+1`, чтобы никто не загадал слово самому себе
- Каждый игрок:
  - загадывает слово определённому игроку
  - **не видит слово, загаданное ему**
  - видит слова других игроков
- Есть возможность перезапуска раунда (только хост)
- У каждого игрока есть личные заметки (хранятся локально в браузере)

---

## 🛠 Технологии

- **Frontend:** Next.js (App Router), React, TypeScript
- **Backend:** Node.js, Express, Socket.IO
- **Realtime:** WebSocket (Socket.IO)
- **Хранение состояния:** in-memory (на сервере)
- **Личные заметки:** localStorage (клиент)

---

## 📂 Структура проекта

.
├── backend/ # Сервер (Socket.IO, логика игры)
│ └── index.js
│
├── frontend/ # Next.js приложение
│ ├── src/
│ └── app/
│
└── .gitignore


## ▶ Запуск проекта локально

### 1️⃣ Backend

```bash
cd backend
npm install
node index.js
```

http://localhost:4000

### Frontend

```bash
cd frontend
npm install
npm run dev
```

http://localhost:3000